{
  "contract": "galy-pm-v1",
  "description": "The project-management verbs the Galy MCP server exposes to your agent. Strictly outward: your assistant reads strategy, briefs and specs and writes back statuses, plans and follow-ups. No verb accepts source code, a diff, or file content — Galy never sees your repository. And no verb sends anything out of your tenant: everything written here stays in your own instance. Support is blind by construction — nothing in this contract can change that, and no skill asks a user to.",
  "transport": {
    "type": "http",
    "url": "https://<your-workspace>.galy.cloud/mcp (every workspace answers on its own host; registered per project by galy-setup, never hardcoded)",
    "auth": "Bearer token in the Authorization header (posted by galy-setup); token is looked up by its SHA-256, so it never travels or is stored in clear on the server"
  },
  "workflow_options": {
    "description": "The complete vocabulary of workflow preferences this kit knows: which skill owns which option, and the canonical values each accepts. This is the single authority - the enums on workflow_default_set, workflow_default_unset and workflow_policy_resolve are checked against it, and so is every option a skill cites. An option that is not here does not exist.",
    "options": {
      "feature-implement": {
        "merge_mode": [
          "auto-merge",
          "stop-before-merge",
          "ask"
        ]
      },
      "ship": {
        "auto_ship": [
          "confident",
          "always-manual",
          "ask"
        ],
        "release_trigger": [
          "merge-ships",
          "separate-call",
          "ask"
        ],
        "release_hold": [
          "hold-for-a-human",
          "go-when-green",
          "ask"
        ],
        "rollback_mode": [
          "revert-and-reship",
          "redeploy-previous",
          "no-way-back",
          "ask"
        ]
      }
    }
  },
  "rest_api": {
    "description": "The REST surface the `galy` CLI uses (gooal PmContentController), same Bearer auth. Read side only + the markdown body round-trip. Content bodies are raw markdown whose sections are delimited by <!-- @field <name> --> and composed/parsed server-side.",
    "base": "https://<your-workspace>.galy.cloud",
    "routes": [
      {
        "method": "GET",
        "path": "/api/pm/search?q=<q>",
        "returns": "{ briefs: [summary], specs: [summary] }"
      },
      {
        "method": "GET",
        "path": "/api/pm/brief/{id}",
        "returns": "{ brief, user_stories }"
      },
      {
        "method": "GET",
        "path": "/api/pm/spec/{id}",
        "returns": "{ spec, phases, risks, acceptance_tests }"
      },
      {
        "method": "GET",
        "path": "/api/pm/content/{type}/{id}/body",
        "returns": "text/markdown (fields: brief = executive/problem/vision/hors-scope, spec = executive/problem/solution)"
      },
      {
        "method": "PUT",
        "path": "/api/pm/content/{type}/{id}/body",
        "body": "{ \"Body\": \"<markdown>\" }",
        "note": "a field absent from the submitted markdown is left unchanged; type = feature-brief | feature-spec"
      }
    ]
  },
  "conventions": {
    "return_envelope": "Every tool returns a JSON string { success: bool, ... } and { success: false, error: string } on failure.",
    "bodies": "Long markdown bodies (brief problem/vision, spec executive summary/problem/solution) are never passed as tool arguments — read and write them with the `galy content pull|push <type> <id>` CLI. Tools carry metadata only (title, phases, status, risks).",
    "forbidden_fields": "No tool declares a parameter named code, diff, patch, file_content or source_code. The flow is outward-only."
  },
  "tools": [
    {
      "name": "whoami",
      "kind": "read",
      "description": "Identify the authenticated Galy user and workspace for this session (resolved from the token, no parameter). Use it for the user_id that feature_spec_pick and other per-user tools expect — and for the workspace mark, which callers outside a browser have no other way to learn: logo_url and accent_color are null when the workspace has declared none, meaning \"use the product's own mark\", never \"use none\".",
      "params": [],
      "returns": {
        "success": "bool",
        "user_id": "int",
        "email": "string",
        "login": "string",
        "display_name": "string|null",
        "tenant": {
          "id": "int",
          "name": "string",
          "slug": "string",
          "logo_url": "string|null",
          "accent_color": "string|null"
        }
      }
    },
    {
      "name": "strategy_list_periods",
      "kind": "read",
      "description": "List all strategy periods (annual, quarterly).",
      "params": [
        {
          "name": "periodType",
          "type": "string",
          "required": false,
          "description": "Filter: 'annual' or 'quarterly'."
        }
      ],
      "returns": {
        "success": "bool",
        "total": "int",
        "periods": "[{ id, name, period_type, start_date, end_date, parent_period_id, parent_period_name, child_count }]"
      }
    },
    {
      "name": "strategy_navigate_children",
      "kind": "read",
      "description": "Drill down one level (or up to depth=3) inside the objectives tree without loading the whole strategy. Pass periodId for the period's root objectives, OR objectiveId for that objective's children. Mutually exclusive.",
      "params": [
        {
          "name": "periodId",
          "type": "int",
          "required": false,
          "description": "List the period's root objectives. Required if objectiveId is omitted."
        },
        {
          "name": "objectiveId",
          "type": "int",
          "required": false,
          "description": "List this objective's direct children. Required if periodId is omitted."
        },
        {
          "name": "depth",
          "type": "int",
          "required": false,
          "default": 1,
          "description": "Levels to expand: 1..3 (hard cap)."
        },
        {
          "name": "includeKrs",
          "type": "bool",
          "required": false,
          "default": false,
          "description": "Include key results per objective."
        }
      ],
      "returns": {
        "success": "bool",
        "period_id": "int?",
        "objective_id": "int?",
        "depth": "int",
        "total": "int",
        "items": "[{ id, title, icon, objective_type, parent_objective_id, is_linked, computed_progress, child_count, has_children, key_result_count, key_results? }]"
      }
    },
    {
      "name": "strategy_search_objectives",
      "kind": "read",
      "description": "Search objectives by title. Use it when the user names an objective but you have no id; prefer strategy_navigate_children once you have an anchor.",
      "params": [
        {
          "name": "q",
          "type": "string",
          "required": true,
          "description": "Search term matched against objective titles."
        }
      ],
      "returns": {
        "success": "bool",
        "objectives": "[objective]"
      }
    },
    {
      "name": "strategy_create_objective",
      "kind": "write",
      "description": "Create a strategy objective, at the root of a period or under a parent. The audit uses it to give its work a home when the team has none yet — and asks before calling it: an objective is the team's strategy, not the audit's own output.",
      "params": [
        {
          "name": "title",
          "type": "string",
          "required": true,
          "description": "Objective title, in the user's language."
        },
        {
          "name": "period_id",
          "type": "int",
          "required": false,
          "description": "Period it belongs to. Defaults to the current one."
        },
        {
          "name": "parent_objective_id",
          "type": "int",
          "required": false,
          "description": "Parent objective. Null for a root objective — this is what makes a sub-objective a single call."
        },
        {
          "name": "description_md",
          "type": "string",
          "required": false
        },
        {
          "name": "owner_user_id",
          "type": "int",
          "required": false
        }
      ],
      "returns": {
        "success": "bool",
        "objective": "objective"
      }
    },
    {
      "name": "strategy_get_objective_breadcrumb",
      "kind": "read",
      "description": "Return the parent chain of an objective from root to leaf, one node per level. Use it to build the objective header of a spec without loading the whole strategy.",
      "params": [
        {
          "name": "objectiveId",
          "type": "int",
          "required": true,
          "description": "Objective whose ancestor chain we want."
        },
        {
          "name": "includeVision",
          "type": "bool",
          "required": false,
          "default": false,
          "description": "Include the top ambition/vision tier. Default false (stop at the annual root)."
        }
      ],
      "returns": {
        "success": "bool",
        "objective_id": "int",
        "chain": "[{ id, title, icon, objective_type, period_id, period_name }]"
      }
    },
    {
      "name": "strategy_my_okrs",
      "kind": "read",
      "description": "The objectives someone is accountable for, with their key results, their progress and the LAST CHECK-IN of each. One call for 'where do my key results stand?', and the same selection the 'My OKRs' screen shows, so an agent and the page cannot answer differently. Every key result carries days_since_check_in: a figure at 40% reads the same whether it was reported yesterday or never, and that is what tells the two apart.",
      "params": [
        {
          "name": "owner_user_id",
          "type": "int",
          "required": false,
          "description": "Whose objectives. Defaults to the authenticated user."
        },
        {
          "name": "period_id",
          "type": "int",
          "required": false,
          "description": "Which period. Left out, the latest one."
        },
        {
          "name": "team_id",
          "type": "int",
          "required": false,
          "description": "A whole team's load instead of one person's. A team wins over the user."
        }
      ],
      "returns": {
        "success": "bool",
        "owner_user_id": "int",
        "period": "period?",
        "objectives": "[{ objective, parent_title, key_results: [{ id, title, unit, start_value, target_value, current_value, computed_progress, last_check_in, days_since_check_in }] }]"
      }
    },
    {
      "name": "strategy_check_in_history",
      "kind": "read",
      "description": "The recorded check-ins of one key result, most recent first, each with who reported it. Answers 'is this figure really moving?', which the value of the day cannot.",
      "params": [
        {
          "name": "key_result_id",
          "type": "int",
          "required": true
        },
        {
          "name": "take",
          "type": "int",
          "required": false,
          "default": 20,
          "description": "How many, most recent first (1..100)."
        }
      ],
      "returns": {
        "success": "bool",
        "key_result_id": "int",
        "check_ins": "[{ id, previous_value, new_value, confidence, comment, author_user_id, author_name, date_created }]"
      }
    },
    {
      "name": "strategy_create_check_in",
      "kind": "write",
      "description": "Record where a key result stands today: the value, the confidence and one sentence. This is the normal way to move a figure — it leaves a dated trace and refreshes every gauge above it. The author is resolved from the token, so a check-in is always signed.",
      "params": [
        {
          "name": "key_result_id",
          "type": "int",
          "required": true
        },
        {
          "name": "new_value",
          "type": "decimal",
          "required": true,
          "description": "The value today. It comes from the user, never from a deduction."
        },
        {
          "name": "confidence",
          "type": "string",
          "required": false,
          "default": "on_track",
          "description": "on_track | at_risk | off_track — the owner's reading, not the agent's."
        },
        {
          "name": "comment",
          "type": "string",
          "required": false,
          "description": "What explains the figure, in one or two sentences."
        }
      ],
      "returns": {
        "success": "bool",
        "key_result": "key_result",
        "objective_progress": "[{ objective_id, progress }]"
      }
    },
    {
      "name": "strategy_update_key_result",
      "kind": "write",
      "description": "Correct a key result's definition — title, unit, starting value, target — or fix a current value entered by mistake. A figure that has genuinely MOVED is recorded with strategy_create_check_in instead, which leaves the trace. Only the fields passed are changed.",
      "params": [
        {
          "name": "key_result_id",
          "type": "int",
          "required": true
        },
        {
          "name": "title",
          "type": "string",
          "required": false
        },
        {
          "name": "metric_type",
          "type": "string",
          "required": false,
          "description": "number | percent | currency | boolean"
        },
        {
          "name": "unit",
          "type": "string",
          "required": false
        },
        {
          "name": "start_value",
          "type": "decimal",
          "required": false
        },
        {
          "name": "target_value",
          "type": "decimal",
          "required": false
        },
        {
          "name": "current_value",
          "type": "decimal",
          "required": false,
          "description": "A correction, not a move. To move it, use strategy_create_check_in."
        },
        {
          "name": "objective_id",
          "type": "int",
          "required": false,
          "description": "Attach it to another objective."
        },
        {
          "name": "previous_value_type",
          "type": "string",
          "required": false,
          "description": "period_start | previous_period."
        }
      ],
      "returns": {
        "success": "bool",
        "key_result": "key_result"
      }
    },
    {
      "name": "feature_brief_create",
      "kind": "write",
      "description": "Create a new feature brief (business framing: title, domain, objective). Returns the new brief id. Write the body (problem/vision/executive summary) with `galy content push feature-brief <id>`, not as arguments.",
      "params": [
        {
          "name": "title",
          "type": "string",
          "required": true
        },
        {
          "name": "domain",
          "type": "string",
          "required": false,
          "default": "product",
          "description": "marketing | product | internal | editorial"
        },
        {
          "name": "periodId",
          "type": "int",
          "required": false
        },
        {
          "name": "objectiveId",
          "type": "int",
          "required": false
        },
        {
          "name": "ownerUserId",
          "type": "int",
          "required": false,
          "description": "Omit to default to the authenticated user."
        },
        {
          "name": "nextFollowupDate",
          "type": "string",
          "required": false,
          "description": "ISO YYYY-MM-DD."
        }
      ],
      "returns": {
        "success": "bool",
        "brief_id": "int"
      }
    },
    {
      "name": "feature_brief_get",
      "kind": "read",
      "description": "Get a feature brief by id, with linked objectives, child specs, owner/team names.",
      "params": [
        {
          "name": "briefId",
          "type": "int",
          "required": true
        }
      ],
      "returns": {
        "success": "bool",
        "brief": "object"
      }
    },
    {
      "name": "feature_brief_list",
      "kind": "read",
      "description": "List feature briefs with optional filters (period, owner, status, domain, keyword, date windows). Metadata only — no body.",
      "params": [
        {
          "name": "periodId",
          "type": "int",
          "required": false
        },
        {
          "name": "ownerUserId",
          "type": "int",
          "required": false
        },
        {
          "name": "statusFilter",
          "type": "string",
          "required": false,
          "description": "Draft | Accepted | InProgress | Done | Canceled"
        },
        {
          "name": "domainFilter",
          "type": "string",
          "required": false
        },
        {
          "name": "query",
          "type": "string",
          "required": false,
          "description": "Keyword search (title + body, case-insensitive)."
        },
        {
          "name": "take",
          "type": "int",
          "required": false,
          "default": 60,
          "description": "Max 150."
        }
      ],
      "returns": {
        "success": "bool",
        "total": "int",
        "returned": "int",
        "truncated": "bool",
        "briefs": "object[]"
      }
    },
    {
      "name": "feature_brief_update",
      "kind": "write",
      "description": "Update brief metadata (title/domain/status/priority/objective/nextFollowup). Pass null to leave a field unchanged. For the body, use the content CLI.",
      "params": [
        {
          "name": "briefId",
          "type": "int",
          "required": true
        },
        {
          "name": "title",
          "type": "string",
          "required": false
        },
        {
          "name": "domain",
          "type": "string",
          "required": false
        },
        {
          "name": "status",
          "type": "string",
          "required": false
        },
        {
          "name": "priority",
          "type": "string",
          "required": false
        },
        {
          "name": "nextFollowupDate",
          "type": "string",
          "required": false
        },
        {
          "name": "objectiveId",
          "type": "int",
          "required": false
        }
      ],
      "returns": {
        "success": "bool"
      }
    },
    {
      "name": "feature_brief_add_user_story",
      "kind": "write",
      "description": "Add a business user story to a brief (persona / action / benefit). A brief carries no implementation effort.",
      "params": [
        {
          "name": "briefId",
          "type": "int",
          "required": true
        },
        {
          "name": "persona",
          "type": "string",
          "required": true
        },
        {
          "name": "action",
          "type": "string",
          "required": true
        },
        {
          "name": "benefit",
          "type": "string",
          "required": true
        },
        {
          "name": "priority",
          "type": "string",
          "required": false,
          "default": "P1",
          "description": "P0 | P1 | P2"
        }
      ],
      "returns": {
        "success": "bool",
        "user_story_id": "int"
      }
    },
    {
      "name": "feature_spec_create",
      "kind": "write",
      "description": "Create a technical spec under a brief (1..N specs per brief). Write the body with `galy content push feature-spec <id>`, not as arguments.",
      "params": [
        {
          "name": "featureBriefId",
          "type": "int",
          "required": true
        },
        {
          "name": "title",
          "type": "string",
          "required": true
        },
        {
          "name": "scope",
          "type": "string",
          "required": false,
          "default": "full",
          "description": "front | back | batch | mcp | full"
        },
        {
          "name": "category",
          "type": "string",
          "required": false,
          "default": "backoffice"
        },
        {
          "name": "initialEstimateHours",
          "type": "decimal",
          "required": false
        }
      ],
      "returns": {
        "success": "bool",
        "spec_id": "int"
      }
    },
    {
      "name": "feature_spec_get",
      "kind": "read",
      "description": "Get a spec by id, with phases, risks, acceptance tests, lead name.",
      "params": [
        {
          "name": "specId",
          "type": "int",
          "required": true
        }
      ],
      "returns": {
        "success": "bool",
        "spec": "object"
      }
    },
    {
      "name": "feature_spec_list",
      "kind": "read",
      "description": "List specs with optional filters (brief, status, lead, keyword, date windows). Metadata only — no body, no nested phases/risks.",
      "params": [
        {
          "name": "featureBriefId",
          "type": "int",
          "required": false
        },
        {
          "name": "statusFilter",
          "type": "string",
          "required": false
        },
        {
          "name": "leadUserId",
          "type": "int",
          "required": false
        },
        {
          "name": "query",
          "type": "string",
          "required": false
        },
        {
          "name": "take",
          "type": "int",
          "required": false,
          "default": 60,
          "description": "Max 150."
        }
      ],
      "returns": {
        "success": "bool",
        "total": "int",
        "returned": "int",
        "truncated": "bool",
        "specs": "object[]"
      }
    },
    {
      "name": "feature_spec_pick",
      "kind": "write",
      "description": "Atomically claim a spec for a user (lead = user, status = InProgress). success=false if already claimed by another user.",
      "params": [
        {
          "name": "specId",
          "type": "int",
          "required": true
        },
        {
          "name": "userId",
          "type": "int",
          "required": false,
          "description": "Omit to default to the authenticated user."
        }
      ],
      "returns": {
        "success": "bool",
        "current_lead_user_id": "int?",
        "current_lead_name": "string?",
        "current_status": "string?"
      }
    },
    {
      "name": "feature_spec_update",
      "kind": "write",
      "description": "Update spec metadata (title/scope/category/status/priority/estimates) or reassign to another brief. Pass null to leave a field unchanged. For the body, use the content CLI.",
      "params": [
        {
          "name": "specId",
          "type": "int",
          "required": true
        },
        {
          "name": "title",
          "type": "string",
          "required": false
        },
        {
          "name": "scope",
          "type": "string",
          "required": false
        },
        {
          "name": "category",
          "type": "string",
          "required": false
        },
        {
          "name": "status",
          "type": "string",
          "required": false,
          "description": "NotStarted | InProgress | Review | Done | Canceled"
        },
        {
          "name": "priority",
          "type": "string",
          "required": false
        },
        {
          "name": "initialEstimateHours",
          "type": "decimal",
          "required": false
        },
        {
          "name": "currentEstimateHours",
          "type": "decimal",
          "required": false
        },
        {
          "name": "featureBriefId",
          "type": "int",
          "required": false
        }
      ],
      "returns": {
        "success": "bool"
      }
    },
    {
      "name": "feature_spec_complete",
      "kind": "write",
      "description": "Mark a whole spec Done (status = Done, done_at = now). Optionally store the URL of the delivered change (e.g. your PR).",
      "params": [
        {
          "name": "specId",
          "type": "int",
          "required": true
        },
        {
          "name": "prUrl",
          "type": "string",
          "required": false,
          "description": "URL of the delivered change on your side (optional)."
        }
      ],
      "returns": {
        "success": "bool"
      }
    },
    {
      "name": "feature_spec_add_phase",
      "kind": "write",
      "description": "Add an implementation phase to a spec (title + plan + estimate).",
      "params": [
        {
          "name": "specId",
          "type": "int",
          "required": true
        },
        {
          "name": "title",
          "type": "string",
          "required": true
        },
        {
          "name": "objectiveMd",
          "type": "string",
          "required": false
        },
        {
          "name": "actionPlanMd",
          "type": "string",
          "required": false
        },
        {
          "name": "validationCriterionMd",
          "type": "string",
          "required": false
        },
        {
          "name": "estimateHours",
          "type": "decimal",
          "required": false
        }
      ],
      "returns": {
        "success": "bool",
        "phase_id": "int"
      }
    },
    {
      "name": "feature_spec_update_phase",
      "kind": "write",
      "description": "Edit an existing phase (amend/correct after creation). Pass null to leave a field unchanged; empty string clears a markdown field. Use feature_spec_set_phase_status for status.",
      "params": [
        {
          "name": "phaseId",
          "type": "int",
          "required": true
        },
        {
          "name": "title",
          "type": "string",
          "required": false
        },
        {
          "name": "objectiveMd",
          "type": "string",
          "required": false
        },
        {
          "name": "actionPlanMd",
          "type": "string",
          "required": false
        },
        {
          "name": "validationCriterionMd",
          "type": "string",
          "required": false
        },
        {
          "name": "estimateHours",
          "type": "decimal",
          "required": false
        },
        {
          "name": "rafHours",
          "type": "decimal",
          "required": false
        }
      ],
      "returns": {
        "success": "bool"
      }
    },
    {
      "name": "feature_spec_set_phase_status",
      "kind": "write",
      "description": "Mark a phase NotStarted | InProgress | Done. A delivery URL is stored when Done.",
      "params": [
        {
          "name": "phaseId",
          "type": "int",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "required": true,
          "description": "NotStarted | InProgress | Done"
        },
        {
          "name": "prUrl",
          "type": "string",
          "required": false,
          "description": "Delivery URL, used when status=Done."
        }
      ],
      "returns": {
        "success": "bool"
      }
    },
    {
      "name": "feature_spec_add_risk",
      "kind": "write",
      "description": "Add a typed risk to a spec (Technical|Business|Timeline + severity + probability + mitigation).",
      "params": [
        {
          "name": "specId",
          "type": "int",
          "required": true
        },
        {
          "name": "label",
          "type": "string",
          "required": true
        },
        {
          "name": "riskType",
          "type": "string",
          "required": false,
          "default": "Technical",
          "description": "Technical | Business | Timeline"
        },
        {
          "name": "severity",
          "type": "string",
          "required": false,
          "default": "medium",
          "description": "low | medium | high | critical | mitigated"
        },
        {
          "name": "probability",
          "type": "string",
          "required": false,
          "default": "medium",
          "description": "low | medium | high"
        },
        {
          "name": "mitigation",
          "type": "string",
          "required": false
        }
      ],
      "returns": {
        "success": "bool",
        "risk_id": "int"
      }
    },
    {
      "name": "feature_spec_add_acceptance_test",
      "kind": "write",
      "description": "Add a technical acceptance test — your assistant verifies it at end of dev. Kind visual|nonvisual; verificationMd holds the URL/command/query to run (no source code).",
      "params": [
        {
          "name": "specId",
          "type": "int",
          "required": true
        },
        {
          "name": "kind",
          "type": "string",
          "required": true,
          "description": "visual | nonvisual"
        },
        {
          "name": "label",
          "type": "string",
          "required": true
        },
        {
          "name": "verificationMd",
          "type": "string",
          "required": true,
          "description": "How to verify: a URL to open, a command to run, a query to check."
        }
      ],
      "returns": {
        "success": "bool",
        "acceptance_test_id": "int"
      }
    },
    {
      "name": "feature_spec_update_acceptance_test",
      "kind": "write",
      "description": "Edit an existing tech acceptance test (kind, label, verification). Pass null to leave a field unchanged; empty string clears verification.",
      "params": [
        {
          "name": "acceptanceTestId",
          "type": "int",
          "required": true
        },
        {
          "name": "kind",
          "type": "string",
          "required": false
        },
        {
          "name": "label",
          "type": "string",
          "required": false
        },
        {
          "name": "verificationMd",
          "type": "string",
          "required": false
        }
      ],
      "returns": {
        "success": "bool"
      }
    },
    {
      "name": "workflow_catalog_list",
      "kind": "read",
      "description": "List the workflow options THIS instance actually knows: skill, option, accepted values, and the value that applies when nobody has decided. Read it instead of hardcoding option names - an option this instance does not know is a setting nothing will ever honour, and a value it does not accept is one it will refuse to store. It is also what closes the loop on this contract: the vocabulary declared here is compared to the one the instance serves, so a drift on either side is caught instead of being discovered by a user toggling a control that changes nothing.",
      "params": [],
      "returns": {
        "success": "bool",
        "workflow_options": "[{ skill, option, values: [string], default_when_unset: string }]"
      }
    },
    {
      "name": "workflow_policy_resolve",
      "kind": "read",
      "description": "Collapse the workspace policy and the user's preference for one skill+option into a single answer, and say who decided. The administrator's policy (allow | deny | user_choice) outranks the user's value; a skill must call this before offering any choice, or it offers a control the user may not have. An unreadable policy is never permission.",
      "params": [
        {
          "name": "skill",
          "type": "string",
          "required": true,
          "description": "Skill name without the leading slash.",
          "enum": [
            "feature-implement",
            "ship"
          ]
        },
        {
          "name": "option",
          "type": "string",
          "required": true,
          "description": "Option name. Must belong to the skill named above - see workflow_options.",
          "enum": [
            "auto_ship",
            "merge_mode",
            "release_hold",
            "release_trigger",
            "rollback_mode"
          ]
        }
      ],
      "returns": {
        "success": "bool",
        "effective": "allow | deny | ask",
        "decided_by": "admin | user | default",
        "admin_policy": "allow | deny | user_choice | null",
        "user_value": "string | null",
        "settings_url": "string (the page on the user's Galy account where both layers are visible)"
      }
    },
    {
      "name": "workflow_default_get_all",
      "kind": "read",
      "description": "Return every workflow preference stored for the current user, as {skill, option, value}. Skills read this once at the start of a run; empty means use the skill's built-in default.",
      "params": [],
      "returns": {
        "success": "bool",
        "user_id": "int",
        "defaults": "[{ skill, option, value }]"
      }
    },
    {
      "name": "workflow_default_set",
      "kind": "write",
      "description": "Persist (upsert) one workflow preference for the current user. value is the canonical machine string the skill expects (e.g. 'on', 'off', 'ask').",
      "params": [
        {
          "name": "skill",
          "type": "string",
          "required": true,
          "description": "Skill name without the leading slash.",
          "enum": [
            "feature-implement",
            "ship"
          ]
        },
        {
          "name": "option",
          "type": "string",
          "required": true,
          "description": "Option key. Must belong to the skill named above - see workflow_options.",
          "enum": [
            "auto_ship",
            "merge_mode",
            "release_hold",
            "release_trigger",
            "rollback_mode"
          ]
        },
        {
          "name": "value",
          "type": "string",
          "required": true,
          "description": "Canonical machine value accepted by that option - see workflow_options. Never a localized label. The instance refuses a value it does not accept for an option it knows, so read workflow_catalog_list rather than guess.",
          "enum": [
            "always-manual",
            "ask",
            "auto-merge",
            "confident",
            "go-when-green",
            "hold-for-a-human",
            "merge-ships",
            "no-way-back",
            "redeploy-previous",
            "revert-and-reship",
            "separate-call",
            "stop-before-merge"
          ]
        }
      ],
      "returns": {
        "success": "bool",
        "user_id": "int",
        "skill": "string",
        "option": "string",
        "value": "string"
      }
    },
    {
      "name": "workflow_default_unset",
      "kind": "write",
      "description": "Remove one workflow preference so the skill falls back to its built-in default.",
      "params": [
        {
          "name": "skill",
          "type": "string",
          "required": true,
          "enum": [
            "feature-implement",
            "ship"
          ]
        },
        {
          "name": "option",
          "type": "string",
          "required": true,
          "enum": [
            "auto_ship",
            "merge_mode",
            "release_hold",
            "release_trigger",
            "rollback_mode"
          ]
        }
      ],
      "returns": {
        "success": "bool",
        "user_id": "int",
        "skill": "string",
        "option": "string"
      }
    },
    {
      "name": "followup_check_add",
      "kind": "write",
      "description": "Add a post-delivery follow-up check to a spec (check_type=technical) OR a brief (check_type=business) — exactly one parent. followupPromptMd holds the executable instructions + explicit pass/fail thresholds, written upfront and human-reviewed.",
      "params": [
        {
          "name": "checkType",
          "type": "string",
          "required": true,
          "description": "technical | business"
        },
        {
          "name": "title",
          "type": "string",
          "required": true
        },
        {
          "name": "followupPromptMd",
          "type": "string",
          "required": true,
          "description": "Instructions + pass/fail thresholds (markdown)."
        },
        {
          "name": "scheduleOffsetDays",
          "type": "int",
          "required": true,
          "description": "First run at delivered_at + N days."
        },
        {
          "name": "featureSpecId",
          "type": "int",
          "required": false,
          "description": "Technical check — pass this OR featureBriefId, never both."
        },
        {
          "name": "featureBriefId",
          "type": "int",
          "required": false,
          "description": "Business check — pass this OR featureSpecId, never both."
        },
        {
          "name": "chainOffsetDays",
          "type": "int",
          "required": false,
          "description": "Next horizon materialized on pass (null = no chain)."
        },
        {
          "name": "onFailAction",
          "type": "string",
          "required": false,
          "description": "bug_fix | implement_spec | create_spec"
        }
      ],
      "returns": {
        "success": "bool",
        "followup_check_id": "int"
      }
    },
    {
      "name": "followup_check_list",
      "kind": "read",
      "description": "List active follow-up checks for a spec or a brief (ordered by display order).",
      "params": [
        {
          "name": "featureSpecId",
          "type": "int",
          "required": false
        },
        {
          "name": "featureBriefId",
          "type": "int",
          "required": false
        }
      ],
      "returns": {
        "success": "bool",
        "checks": "object[]"
      }
    },
    {
      "name": "followup_check_update",
      "kind": "write",
      "description": "Edit an existing follow-up check. Pass null to leave a field unchanged; empty string clears the prompt. Set isActive=false to deactivate.",
      "params": [
        {
          "name": "checkId",
          "type": "int",
          "required": true
        },
        {
          "name": "title",
          "type": "string",
          "required": false
        },
        {
          "name": "followupPromptMd",
          "type": "string",
          "required": false
        },
        {
          "name": "scheduleOffsetDays",
          "type": "int",
          "required": false
        },
        {
          "name": "chainOffsetDays",
          "type": "int",
          "required": false
        },
        {
          "name": "onFailAction",
          "type": "string",
          "required": false
        },
        {
          "name": "isActive",
          "type": "bool",
          "required": false
        }
      ],
      "returns": {
        "success": "bool"
      }
    },
    {
      "name": "acceptance_open",
      "kind": "write",
      "description": "Open an acceptance pass on a running feature — one pass = one branch = one pull request. Idempotent on the branch: called again it hands back the pass already open, so a session that crashed and came back does not fork the queue in two. STAYS IN YOUR WORKSPACE, like every verb here.",
      "params": [
        {
          "name": "title",
          "type": "string",
          "required": true,
          "description": "What is being reviewed, in the reviewer's own words — not a date and not a branch name."
        },
        {
          "name": "branch_name",
          "type": "string",
          "required": false,
          "description": "The single branch this pass commits to."
        },
        {
          "name": "feature_spec_id",
          "type": "int",
          "required": false
        },
        {
          "name": "feature_brief_id",
          "type": "int",
          "required": false
        }
      ],
      "returns": {
        "success": "bool",
        "session_id": "int",
        "session": "object"
      }
    },
    {
      "name": "acceptance_add_remark",
      "kind": "write",
      "description": "Add one remark to the queue. Persisted immediately as queued, at the back. Returns at once — never blocks, never waits for the remark to be dealt with, because the person firing remarks is looking at the product and not at a terminal. Carries the remark only: no code, no diff, no file content.",
      "params": [
        {
          "name": "session_id",
          "type": "int",
          "required": true
        },
        {
          "name": "title",
          "type": "string",
          "required": true,
          "description": "The remark, as short as it was said."
        },
        {
          "name": "description_md",
          "type": "string",
          "required": false,
          "description": "Any detail worth keeping."
        }
      ],
      "returns": {
        "success": "bool",
        "remark_id": "int",
        "entry_order": "int"
      }
    },
    {
      "name": "acceptance_list",
      "kind": "read",
      "description": "Without session_id: the acceptance passes, open ones first. With session_id: that pass's remarks, in the order they were received.",
      "params": [
        {
          "name": "session_id",
          "type": "int",
          "required": false,
          "description": "List this pass's remarks instead of the passes."
        },
        {
          "name": "status",
          "type": "string",
          "required": false,
          "description": "queued | in_progress | awaiting_user | done | wont_fix | failed"
        }
      ],
      "returns": {
        "success": "bool",
        "sessions": "array",
        "remarks": "array"
      }
    },
    {
      "name": "acceptance_claim_next",
      "kind": "write",
      "description": "Take the oldest remark waiting, or one whose lease has run out, and hold it under a new lease. Steps over remarks parked on a question — the queue must not stall behind a decision that belongs to a person. Returns claimed=false when there is nothing to do.",
      "params": [
        {
          "name": "session_id",
          "type": "int",
          "required": true
        },
        {
          "name": "claimed_by",
          "type": "string",
          "required": true,
          "description": "Who is taking it — a hostname, a session name."
        },
        {
          "name": "lease_minutes",
          "type": "int",
          "required": false,
          "description": "Minutes before the claim expires and the remark can be taken by someone else (default 15)."
        }
      ],
      "returns": {
        "success": "bool",
        "claimed": "bool",
        "remark": "object"
      }
    },
    {
      "name": "acceptance_resolve",
      "kind": "write",
      "description": "Close a remark. done, with the commit that fixed it. wont_fix when it was looked at and deliberately left alone — put the reason in result_md, because what you fixed is readable in the commits and what you knowingly chose NOT to fix is readable nowhere else. failed when it genuinely could not be fixed, reason in result_md too.",
      "params": [
        {
          "name": "remark_id",
          "type": "int",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "required": false,
          "description": "done | wont_fix | failed (default done)"
        },
        {
          "name": "commit_sha",
          "type": "string",
          "required": false
        },
        {
          "name": "result_md",
          "type": "string",
          "required": false,
          "description": "What changed — or why it was left alone."
        }
      ],
      "returns": {
        "success": "bool"
      }
    },
    {
      "name": "acceptance_park",
      "kind": "write",
      "description": "Park a remark on a question only a person can answer. The caller must then move on to the next remark, never wait. Use this only when the answer truly requires a person — anything settleable from the code in under a minute is settled on the spot.",
      "params": [
        {
          "name": "remark_id",
          "type": "int",
          "required": true
        },
        {
          "name": "question_md",
          "type": "string",
          "required": true,
          "description": "The question, written to be read by the person answering."
        }
      ],
      "returns": {
        "success": "bool"
      }
    },
    {
      "name": "acceptance_answer",
      "kind": "write",
      "description": "Answer a parked question. The remark goes back to the queue at its own place, not at the front: the order the person gave still holds, and an answer is not a reason to jump the line.",
      "params": [
        {
          "name": "remark_id",
          "type": "int",
          "required": true
        },
        {
          "name": "answer_md",
          "type": "string",
          "required": true
        }
      ],
      "returns": {
        "success": "bool"
      }
    },
    {
      "name": "acceptance_set_pr",
      "kind": "write",
      "description": "Attach the pass's single pull request. One pass, one PR: a PR per remark buries the reviewer, and a reviewer who skims is a reviewer you no longer have.",
      "params": [
        {
          "name": "session_id",
          "type": "int",
          "required": true
        },
        {
          "name": "pr_number",
          "type": "int",
          "required": true
        },
        {
          "name": "pr_url",
          "type": "string",
          "required": true
        }
      ],
      "returns": {
        "success": "bool"
      }
    },
    {
      "name": "acceptance_close",
      "kind": "write",
      "description": "Close the pass. merged | abandoned (default merged).",
      "params": [
        {
          "name": "session_id",
          "type": "int",
          "required": true
        },
        {
          "name": "status",
          "type": "string",
          "required": false,
          "description": "merged | abandoned"
        }
      ],
      "returns": {
        "success": "bool"
      }
    },
    {
      "name": "onboarding_retro_record",
      "kind": "write",
      "description": "Record the retrospective of an onboarding pass - what worked, what rubbed, the open questions, the suggestions - about the PROCESS, not about the team's work. It is written to the client's own tenant and stays there; no policy guards it, because writing in your own workspace asks nobody's permission. The four fields carry process feedback only: never code, file excerpts, paths, host names, command output, secrets, customer or product names, nor any recorded observation. The server redacts as a net and says so; a redaction that fires means something was written that should not have been. STAYS IN YOUR WORKSPACE, like every verb in this contract. Whether the instance forwards anything to Galy is an instance setting the client's administrator holds, disabled by default - no verb here decides it, and no user is asked.",
      "params": [
        {
          "name": "run_id",
          "type": "int",
          "required": true,
          "description": "The maturity run this pass opened."
        },
        {
          "name": "worked_md",
          "type": "string",
          "required": false,
          "description": "What genuinely helped, about the process."
        },
        {
          "name": "friction_md",
          "type": "string",
          "required": false,
          "description": "Where the pass stalled, asked a bad question, or wasted time."
        },
        {
          "name": "questions_md",
          "type": "string",
          "required": false,
          "description": "What the pass could not answer and should have been able to."
        },
        {
          "name": "suggestions_md",
          "type": "string",
          "required": false,
          "description": "What would have made it better, in the user's own words where they gave them."
        }
      ],
      "returns": {
        "success": "bool",
        "id": "int",
        "redacted": "bool"
      }
    },
    {
      "name": "retro_suggestion_add",
      "kind": "write",
      "description": "Post an end-of-run retrospective suggestion (status pending) — a durable learning about how the assistant should work. Targets the plugin's own skills or your CLAUDE.md, never your product code. The summary is the lesson + the rule to encode. STAYS IN YOUR WORKSPACE, like every verb here. Pick between the two retrospective verbs by SUBJECT, not by destination: a learning about YOUR work belongs here; feedback about the ONBOARDING PROCESS belongs to onboarding_retro_record. Neither leaves your tenant.",
      "params": [
        {
          "name": "source",
          "type": "string",
          "required": true,
          "description": "Originating skill, e.g. feature_implement | ship | feature_followup"
        },
        {
          "name": "target_kind",
          "type": "string",
          "required": true,
          "description": "instruction | skill | command | doc"
        },
        {
          "name": "target_file",
          "type": "string",
          "required": true,
          "description": "Path of the file the rule should live in (a pointer, not its content)."
        },
        {
          "name": "title",
          "type": "string",
          "required": true
        },
        {
          "name": "summary",
          "type": "string",
          "required": true,
          "description": "The lesson + the rule to encode."
        }
      ],
      "returns": {
        "success": "bool",
        "id": "int",
        "status": "string"
      }
    },
    {
      "name": "maturity_challenge",
      "kind": "read",
      "description": "Read where the team's agent-assisted engineering practices stand: level reached, the single next step, what is at risk, and the criteria nobody has ever looked at.",
      "params": [],
      "returns": {
        "success": "bool",
        "level_reached": "int",
        "level_name": "string",
        "observed": "int",
        "total": "int",
        "unverifiable": "int",
        "to_recheck": "int",
        "next_step": "{ criterion, name, power, guard, unlocks } | null",
        "at_risk": "[{ criterion, name, missing_guard }]",
        "never_probed": "string[]",
        "open_questions": "[{ id, criterionId, questionMd, unlocksCount }]",
        "criteria": "[{ criterion, name, level, state (never_probed|observed|stale|partial|absent|unverifiable), unguarded_power, observed_at, expires_at }]",
        "page_url": "string (the maturity page — give it, never build one)"
      }
    },
    {
      "name": "maturity_start_run",
      "kind": "write",
      "description": "Open a maturity pass. Every observation belongs to one.",
      "params": [
        {
          "name": "kind",
          "type": "string",
          "required": false,
          "description": "onboarding (the first pass at a new client) | scheduled | manual. Default: manual."
        }
      ],
      "returns": {
        "success": "bool",
        "run_id": "int",
        "kind": "string",
        "started_at": "datetime",
        "page_url": "string"
      }
    },
    {
      "name": "maturity_run_probes",
      "kind": "write",
      "description": "Run the probes the instance can run on its own — no agent judgement involved. Whatever it cannot see from there is written down as not verifiable, with the reason and who could observe it. Call it before maturity_challenge to work from measured ground rather than from memory.",
      "params": [
        {
          "name": "kind",
          "type": "string",
          "required": false,
          "description": "onboarding | scheduled | manual. Default: scheduled."
        },
        {
          "name": "run_id",
          "type": "int",
          "required": false,
          "description": "Run id from maturity_start_run. Omit it and the probes open a run of their own — a second pass on the page, for the same afternoon."
        }
      ],
      "returns": {
        "success": "bool",
        "runId": "int",
        "observed": "int",
        "partial": "int",
        "absent": "int",
        "unverifiable": "int",
        "total": "int",
        "opened_a_new_run": "bool (true when the run asked for was not found and a fresh one was opened)",
        "page_url": "string"
      }
    },
    {
      "name": "maturity_remediation_get",
      "kind": "read",
      "description": "The written procedure for putting ONE criterion in place — this product's own method, served by the instance. There is no command per criterion and there never will be: twenty criteria would be twenty entries in a menu nobody reads. Fetch it only for a criterion you are about to work on, and only after the user has asked — a red criterion is not consent. Because it comes from the instance, it is the current method even when the caller's plugin is old. A criterion whose procedure is not written yet answers has_procedure:false — say so, and NEVER write one from memory.",
      "params": [
        {
          "name": "criterion_id",
          "type": "string",
          "required": true,
          "description": "Criterion key.",
          "enum": [
            "doctrine_written",
            "infrastructure_described",
            "production_read_only",
            "no_secrets_in_repo",
            "tool_contract",
            "secrets_in_store",
            "environment_bootstraps",
            "strategy_in_system",
            "changes_reviewed",
            "quality_gate_blocks",
            "deploy_traced",
            "rollback_proven",
            "schema_via_toolpath",
            "migrations_reversible",
            "data_fixes_gated",
            "servers_no_shared_password",
            "scheduled_loop_fixes",
            "invariants_monitored",
            "verified_on_user_surface",
            "effect_measured"
          ]
        }
      ],
      "returns": {
        "success": "bool",
        "criterion": "string",
        "name": "string (absent when has_procedure is false)",
        "guard": "string (the rule the procedure serves — absent when has_procedure is false)",
        "has_procedure": "bool",
        "procedure_md": "string (absent when has_procedure is false)",
        "written_for": "string[] (the criteria that DO have one — only when has_procedure is false)",
        "page_url": "string"
      }
    },
    {
      "name": "maturity_record",
      "kind": "write",
      "description": "Record what you OBSERVED for one criterion — never what you assume. Evidence is mandatory except for unverifiable, which needs a reason instead. Never put a secret in the evidence: count and locate, never quote. Read the state it answers with: an unguarded power is stored lower than the state asked for.",
      "params": [
        {
          "name": "criterion_id",
          "type": "string",
          "required": true,
          "description": "Criterion key.",
          "enum": [
            "doctrine_written",
            "infrastructure_described",
            "production_read_only",
            "no_secrets_in_repo",
            "tool_contract",
            "secrets_in_store",
            "environment_bootstraps",
            "strategy_in_system",
            "changes_reviewed",
            "quality_gate_blocks",
            "deploy_traced",
            "rollback_proven",
            "schema_via_toolpath",
            "migrations_reversible",
            "data_fixes_gated",
            "servers_no_shared_password",
            "scheduled_loop_fixes",
            "invariants_monitored",
            "verified_on_user_surface",
            "effect_measured"
          ]
        },
        {
          "name": "state",
          "type": "string",
          "required": true,
          "description": "observed | partial | absent | unverifiable"
        },
        {
          "name": "run_id",
          "type": "int",
          "required": true,
          "description": "Run id from maturity_start_run."
        },
        {
          "name": "evidence_md",
          "type": "string",
          "required": false,
          "description": "What you saw, in plain words: counts, dates, paths. Never a secret value."
        },
        {
          "name": "unverifiable_reason",
          "type": "string",
          "required": false,
          "description": "Why you could not check it. Required when state is unverifiable."
        },
        {
          "name": "unguarded_power",
          "type": "bool",
          "required": false,
          "description": "True when the power exists but you did NOT observe its guard. Forces the state down: an unguarded power never shows green."
        }
      ],
      "returns": {
        "success": "bool",
        "criterionId": "string",
        "state": "string (the state actually STORED, which may be lower than the one requested)",
        "observed_at": "datetime",
        "expires_at": "datetime",
        "evidence_was_redacted": "bool",
        "page_url": "string"
      }
    }
  ]
}
