# Local credentials — never committed
.galy/config.json

# Ephemeral content buffers pulled/pushed by the galy CLI
.tmp/

# Node
node_modules/
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db

# Codex projection — build output of scripts/build-codex.mjs, never a second copy to maintain.
.agents/
.codex/
