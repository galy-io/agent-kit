# Local credentials — never committed. `.galy/` is the folder's former name, still read as a fallback.
.bg/
.galy/

# Ephemeral content buffers pulled/pushed by the bg CLI
.tmp/

# Node
node_modules/
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db

# Codex projection — build output of scripts/build-codex.mjs, never a second copy to maintain.
.agents/
.codex/
